// Create modular image for product images including main image and carousel
// note that images should be able to have an onClick callback passed in
// also note that we should forward refs to the image component